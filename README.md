# نظام حضور وغياب اجتماع ثانوي - كنيسة الملاك روفائيل ⛪

هذا النظام مصمم لإدارة حضور وغياب اجتماع ثانوي بنين، مع دعم كامل للمزامنة السحابية وقاعدة بيانات محلية.

## المميزات الرئيسية ✨
- **تسجيل حضور ذكي**: يتم تحديد تاريخ الجمعة تلقائياً بناءً على الوقت الفعلي.
- **قاعدة بيانات حقيقية**: يستخدم النظام خادم Express مع ملف JSON لتخزين البيانات بشكل دائم.
- **لوحة تحكم (Dashboard)**: عرض إحصائيات دقيقة، نسب الحضور، وتنبيهات الافتقاد والاعتراف.
- **إدارة الشباب**: إضافة، تعديل، وحذف بيانات الشباب بسهولة.
- **دعم كامل للغة العربية**: واجهة مستخدم عصرية وسهلة الاستخدام باللغة العربية.

## متطلبات التشغيل 🛠️
- [Node.js](https://nodejs.org/) (إصدار 18 أو أحدث)
- npm أو yarn

## طريقة التشغيل محلياً 💻

1. **تحميل الكود**:
   ```bash
   git clone <رابط-المستودع>
   cd <اسم-المجلد>
   ```

2. **تثبيت المكتبات**:
   ```bash
   npm install
   ```

3. **تشغيل النظام (وضع التطوير)**:
   ```bash
   npm run dev
   ```
   سيفتح النظام على الرابط: `http://localhost:3000`

4. **بناء النظام للإنتاج**:
   ```bash
   npm run build
   npm start
   ```

## هيكل المشروع 📁
- `server.ts`: الخادم الخلفي (Express) والتعامل مع قاعدة البيانات.
- `App.tsx`: المكون الرئيسي للواجهة الأمامية.
- `pages/`: صفحات النظام (لوحة التحكم، التسجيل، الإعدادات، إلخ).
- `services/storageService.ts`: المسؤول عن التواصل بين الواجهة والخادم.
- `database.json`: ملف قاعدة البيانات (يتم إنشاؤه تلقائياً عند التشغيل).

## ملاحظات هامة ⚠️
- تم إضافة `database.json` إلى ملف `.gitignore` لحماية خصوصية البيانات عند الرفع على GitHub.
- كلمة السر الافتراضية للخدام هي: `kerolos0` (يمكن تغييرها من الإعدادات).

---
**تطوير: كيرلس صفوت**
Angel Raphael Digital Systems 🛡️
